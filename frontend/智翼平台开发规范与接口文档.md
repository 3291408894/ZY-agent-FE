# 智翼（ZhiYi）AI 学习助手平台 — 开发规范与接口文档

> **项目名称**：智翼 AI Agent 学习助手  
> **产品负责人**：卢俊  
> **技术栈**：Vue 3（前端） + FastAPI（后端） + PostgreSQL + Redis  
> **总工作量**：199 故事点 | Sprint 1: 99pts | Sprint 2: 100pts  
> **文档版本**：v1.0 | 2026-07-03

---

## 目录

- [一、项目总览与优先级](#一项目总览与优先级)
- [二、前端开发规范（Vue 3）](#二前端开发规范vue-3)
- [三、后端开发规范（FastAPI）](#三后端开发规范fastapi)
- [四、数据库设计概要](#四数据库设计概要)
- [五、API 接口规范](#五api-接口规范)
- [六、前后端协作规范](#六前后端协作规范)
- [七、Sprint 分工建议](#七sprint-分工建议)
- [附录：错误码与通用响应格式](#附录错误码与通用响应格式)

---

## 一、项目总览与优先级

### 1.1 产品定位

面向 K12 学生的 AI 学习辅助平台，通过自然语言交互让 AI Agent 自动完成课文总结、习题生成与批改、知识图谱构建、文件解析等学习任务。

### 1.2 Sprint 划分

| Sprint | 范围 | 故事点 |
|--------|------|--------|
| Sprint 1 | PBI_01 ~ PBI_07（基础架构 + 核心功能） | 99 |
| Sprint 2 | PBI_05, PBI_08 ~ PBI_12（进阶功能） | 100 |

### 1.3 优先级速查（数字越小越优先）

| 优先级 | PBI | 功能 | 故事点 |
|--------|-----|------|--------|
| **P1** | 01 | 用户注册登录 + 个人档案 | 18 |
| **P1** | 02 | 首页导航 + 功能布局 | 20 |
| **P1** | 03 | UI/UX 护眼主题 + 字体调节 | 5 |
| **P1** | 04 | AI Agent 自然语言交互 | 22 |
| **P1** | 07 | FastAPI 后端基础设施 | 18 |
| **P2** | 06 | 智能课文总结 | 16 |
| **P3** | 05 | 多格式文件上传解析 | 16 |
| **P4** | 08 | 智能习题生成 | 20 |
| **P4** | 10 | 实时批改反馈 | 20 |
| **P5** | 09 | 习题双模式（做题/解析） | 8 |
| **P5** | 12 | AI 思考过程可视化 | 20 |
| **P6** | 11 | 知识图谱可视化 | 16 |

---

## 二、前端开发规范（Vue 3）

### 2.1 技术选型

| 类别 | 选型 | 说明 |
|------|------|------|
| 框架 | Vue 3.4+ (Composition API) | `<script setup lang="ts">` 为标准写法 |
| 语言 | TypeScript 5.x | 所有 `.vue` / `.ts` 文件强制使用 TS |
| 构建 | Vite 5.x | |
| UI 组件库 | Element Plus | 统一 UI 风格，禁止混用其他 UI 库 |
| 状态管理 | Pinia | 替代 Vuex |
| 路由 | Vue Router 4.x | |
| HTTP 客户端 | Axios（统一封装） | 见 2.6 节 |
| CSS 方案 | SCSS + CSS Variables | 主题色、字号等通过 CSS 变量控制 |
| 图表/可视化 | ECharts 5.x | 知识图谱等使用 |
| 代码规范 | ESLint + Prettier | 统一配置，提交前自动格式化 |
| Git Hooks | Husky + lint-staged | |

### 2.2 项目目录结构

```
frontend/
├── public/
│   └── favicon.ico
├── src/
│   ├── api/                    # API 请求层
│   │   ├── request.ts          # Axios 实例、拦截器
│   │   ├── modules/            # 按业务模块拆分
│   │   │   ├── auth.ts         # 认证相关 API
│   │   │   ├── agent.ts        # AI Agent API
│   │   │   ├── summary.ts      # 课文总结 API
│   │   │   ├── exercise.ts     # 习题 API
│   │   │   ├── file.ts         # 文件上传 API
│   │   │   └── knowledge.ts    # 知识图谱 API
│   │   └── types.ts            # 通用请求/响应类型
│   ├── assets/                 # 静态资源
│   │   ├── images/
│   │   └── styles/
│   │       ├── variables.scss  # CSS 变量（主题色、字体等）
│   │       ├── mixins.scss
│   │       └── global.scss     # 全局样式
│   ├── components/             # 公共组件
│   │   ├── common/             # 通用组件（按钮、弹窗等）
│   │   ├── layout/             # 布局组件（Header、Sidebar、Footer）
│   │   └── business/           # 业务组件
│   ├── composables/            # 组合式函数（hooks）
│   │   ├── useAuth.ts
│   │   ├── useTheme.ts
│   │   ├── useFontSize.ts
│   │   └── useSSE.ts           # SSE 流式响应 hook
│   ├── router/
│   │   └── index.ts
│   ├── stores/                 # Pinia 状态管理
│   │   ├── user.ts
│   │   ├── agent.ts
│   │   └── theme.ts
│   ├── views/                  # 页面组件
│   │   ├── auth/               # 登录/注册页
│   │   ├── dashboard/          # 学习仪表盘
│   │   ├── summary/            # 课文总结
│   │   ├── exercise/           # 习题练习
│   │   ├── knowledge/          # 知识图谱
│   │   └── files/              # 文件管理
│   ├── types/                  # 全局 TypeScript 类型
│   │   └── index.ts
│   ├── utils/                  # 工具函数
│   └── App.vue
├── .eslintrc.cjs
├── .prettierrc.json
├── tsconfig.json
├── vite.config.ts
└── package.json
```

### 2.3 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| 组件文件 | PascalCase | `KnowledgeGraph.vue` |
| 业务组件目录 | kebab-case | `file-upload/` |
| TypeScript 文件 | camelCase | `useAuth.ts` |
| 样式文件 | kebab-case | `variables.scss` |
| 变量/函数 | camelCase | `fetchUserProfile()` |
| 常量 | UPPER_SNAKE_CASE | `MAX_FILE_SIZE` |
| 类型/接口 | PascalCase, `I` 前缀表示接口 | `IUser`, `TExerciseType` |
| CSS class | kebab-case, BEM 风格 | `.agent-thought__step--active` |
| 路由 path | kebab-case | `/knowledge-graph` |

### 2.4 组件规范

```typescript
// ✅ 推荐：<script setup lang="ts"> + Composition API
<script setup lang="ts">
import { ref, computed, onMounted } from 'vue'
import type { IExercise } from '@/types'

// Props 定义
interface Props {
  exercises: IExercise[]
  mode: 'practice' | 'review'  // PBI_09：做题模式 / 解析模式
}
const props = withDefaults(defineProps<Props>(), {
  mode: 'practice'
})

// Emits 定义
const emit = defineEmits<{
  (e: 'submit-answer', payload: { exerciseId: string; answer: string }): void
  (e: 'switch-mode', mode: 'practice' | 'review'): void
}>()

// 响应式数据
const currentIndex = ref(0)

// 计算属性
const currentExercise = computed(() => props.exercises[currentIndex.value])
</script>
```

**组件拆分原则**：
- 单一职责：一个组件只做一件事
- 页面组件（`views/`）只负责组装，业务逻辑抽取到 `composables/`
- 可复用 UI 片段抽取到 `components/common/`
- 业务相关但可复用的抽取到 `components/business/`

### 2.5 状态管理（Pinia）

```typescript
// stores/user.ts
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'
import type { IUserProfile } from '@/types'
import { getUserProfile, updateProfile } from '@/api/modules/auth'

export const useUserStore = defineStore('user', () => {
  const profile = ref<IUserProfile | null>(null)
  const isLoggedIn = computed(() => !!profile.value)

  async function fetchProfile() {
    const res = await getUserProfile()
    profile.value = res.data
  }

  async function updateUserProfile(data: Partial<IUserProfile>) {
    await updateProfile(data)
    await fetchProfile()  // 刷新本地状态
  }

  return { profile, isLoggedIn, fetchProfile, updateUserProfile }
})
```

**规则**：
- 所有跨组件共享的状态放入 Pinia Store
- 页面内部状态使用组件级 `ref`/`reactive`
- Store 命名：`useXxxStore`
- **严禁**在组件中直接操作 localStorage 管理全局状态

### 2.6 API 请求封装

```typescript
// api/request.ts
import axios, { type AxiosRequestConfig } from 'axios'
import { useUserStore } from '@/stores/user'
import { ElMessage } from 'element-plus'
import router from '@/router'

const request = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL,  // 环境变量
  timeout: 30000,  // 30s 超时
})

// 请求拦截器：自动附加 Token
request.interceptors.request.use((config) => {
  const token = localStorage.getItem('access_token')
  if (token) {
    config.headers.Authorization = `Bearer ${token}`
  }
  return config
})

// 响应拦截器：统一错误处理
request.interceptors.response.use(
  (res) => res.data,  // 直接返回 data，业务代码无需解包
  (error) => {
    if (error.response?.status === 401) {
      localStorage.removeItem('access_token')
      router.push('/login')
    }
    ElMessage.error(error.response?.data?.message || '请求失败')
    return Promise.reject(error)
  }
)

export default request
```

**API 模块示例**：

```typescript
// api/modules/exercise.ts
import request from '../request'
import type { IExercise, IGenerateExerciseReq, ISubmitAnswerReq, IGradeResult } from '@/types'

export function generateExercise(params: IGenerateExerciseReq) {
  return request.post<IExercise[]>('/api/v1/exercises/generate', params)
}

export function submitAnswer(body: ISubmitAnswerReq) {
  return request.post<IGradeResult>('/api/v1/exercises/grade', body)
}

export function getExerciseHistory(params: { page: number; pageSize: number }) {
  return request.get('/api/v1/exercises/history', { params })
}
```

### 2.7 SSE 流式响应处理（AI Agent 专用）

AI Agent 的响应使用 Server-Sent Events (SSE) 流式传输，前端使用 `EventSource` 或 `fetch` 的 ReadableStream：

```typescript
// composables/useSSE.ts
import { ref } from 'vue'

export function useSSE() {
  const message = ref('')
  const isStreaming = ref(false)
  const error = ref<string | null>(null)

  async function connect(url: string, body: object, onStep?: (step: string) => void) {
    isStreaming.value = true
    message.value = ''
    error.value = null

    const token = localStorage.getItem('access_token')
    const response = await fetch(url, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${token}`,
        'Accept': 'text/event-stream',
      },
      body: JSON.stringify(body),
    })

    const reader = response.body!.getReader()
    const decoder = new TextDecoder()

    while (true) {
      const { done, value } = await reader.read()
      if (done) break

      const chunk = decoder.decode(value)
      // 解析 SSE 格式: "data: {...}\n\n"
      for (const line of chunk.split('\n')) {
        if (line.startsWith('data: ')) {
          const data = JSON.parse(line.slice(6))
          if (data.type === 'thought') {
            onStep?.(data.content)  // PBI_12: 思考步骤回调
          } else if (data.type === 'content') {
            message.value += data.content
          }
        }
      }
    }
    isStreaming.value = false
  }

  return { message, isStreaming, error, connect }
}
```

### 2.8 UI/UX 规范（对应 PBI_03）

**主题色**（CSS 变量，定义在 `variables.scss`）：

```scss
:root {
  // 护眼配色方案 — 柔和蓝白基调
  --color-primary: #5B9BD5;        // 主色：柔和蓝
  --color-primary-light: #E8F0FE;  // 浅蓝背景
  --color-bg: #F5F3EE;             // 米白背景（护眼）
  --color-bg-card: #FFFFFF;
  --color-text: #333333;
  --color-text-secondary: #666666;
  --color-border: #E4E7ED;
  --color-success: #67C23A;
  --color-warning: #E6A23C;
  --color-danger: #F56C6C;

  // 字体（三档可调 —— PBI_03 验收条件2）
  --font-size-small: 13px;
  --font-size-medium: 15px;   // 默认
  --font-size-large: 17px;
}
```

**字体调节实现**（通过 Pinia Store + CSS 变量）：

```typescript
// composables/useFontSize.ts
import { useThemeStore } from '@/stores/theme'

export function useFontSize() {
  const theme = useThemeStore()
  const sizes = { small: '13px', medium: '15px', large: '17px' }

  function setFontSize(level: 'small' | 'medium' | 'large') {
    document.documentElement.style.setProperty('--font-size-base', sizes[level])
    theme.fontSizeLevel = level
  }
  return { setFontSize }
}
```

---

## 三、后端开发规范（FastAPI）

### 3.1 技术选型

| 类别 | 选型 | 说明 |
|------|------|------|
| 框架 | FastAPI 0.110+ | 异步支持，自动 OpenAPI 文档 |
| 语言 | Python 3.12+ | 使用类型注解 |
| ORM | SQLAlchemy 2.0 (async) | 异步引擎 |
| 数据库迁移 | Alembic | |
| 数据校验 | Pydantic v2 | 与 FastAPI 深度集成 |
| 认证 | JWT (python-jose) + bcrypt | |
| 缓存 | Redis (aioredis) | |
| 任务队列 | Celery + Redis | 文件解析等耗时任务 |
| AI 流式 | SSE (sse-starlette) | AI Agent 流式响应 |
| 文件存储 | MinIO / 本地存储 | |
| 日志 | loguru | |
| 测试 | pytest + httpx | API 测试覆盖率 > 80% |
| 代码规范 | ruff (linter) + black (formatter) | |

### 3.2 项目目录结构

```
backend/
├── alembic/                    # 数据库迁移
│   └── versions/
├── app/
│   ├── api/                    # API 路由层
│   │   ├── v1/
│   │   │   ├── __init__.py
│   │   │   ├── router.py       # 聚合所有路由
│   │   │   ├── auth.py         # 认证路由    (PBI_01)
│   │   │   ├── users.py        # 用户路由    (PBI_01)
│   │   │   ├── agent.py        # AI Agent    (PBI_04, PBI_12)
│   │   │   ├── summary.py      # 课文总结    (PBI_06)
│   │   │   ├── exercises.py    # 习题        (PBI_08, PBI_09, PBI_10)
│   │   │   ├── files.py        # 文件管理    (PBI_05)
│   │   │   └── knowledge.py    # 知识图谱    (PBI_11)
│   │   └── deps.py             # 依赖注入（get_db, get_current_user 等）
│   ├── core/
│   │   ├── config.py           # 配置管理（pydantic-settings）
│   │   ├── security.py         # JWT、密码哈希
│   │   ├── database.py         # 数据库引擎 & Session
│   │   └── redis.py            # Redis 连接
│   ├── models/                 # SQLAlchemy 数据模型
│   │   ├── user.py
│   │   ├── summary.py
│   │   ├── exercise.py
│   │   ├── file.py
│   │   └── knowledge.py
│   ├── schemas/                # Pydantic 请求/响应模型
│   │   ├── user.py
│   │   ├── agent.py
│   │   ├── summary.py
│   │   ├── exercise.py
│   │   ├── file.py
│   │   └── knowledge.py
│   ├── services/               # 业务逻辑层
│   │   ├── user_service.py
│   │   ├── agent_service.py
│   │   ├── summary_service.py
│   │   ├── exercise_service.py
│   │   ├── file_service.py
│   │   └── knowledge_service.py
│   ├── ai/                     # AI 相关（LLM 调用）
│   │   ├── llm_client.py       # LLM 客户端封装
│   │   ├── prompts/            # Prompt 模板
│   │   │   ├── summary.py
│   │   │   ├── exercise_gen.py
│   │   │   └── grading.py
│   │   └── agent/
│   │       ├── orchestrator.py # Agent 任务编排（PBI_04）
│   │       └── tools.py        # Agent 可用工具注册
│   └── middleware/
│       ├── cors.py
│       └── logging.py
├── tests/
│   ├── conftest.py
│   └── api/
├── requirements.txt
├── pyproject.toml
├── Dockerfile
└── docker-compose.yml
```

### 3.3 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| 文件名 | snake_case | `exercise_service.py` |
| 函数/方法 | snake_case | `def generate_exercises()` |
| 类 | PascalCase | `class ExerciseService` |
| 常量 | UPPER_SNAKE_CASE | `MAX_FILE_SIZE_MB = 50` |
| 路由前缀 | `/api/v1/` 统一前缀 | |
| URL path | kebab-case | `/api/v1/knowledge-graph` |
| 数据库表 | snake_case 复数 | `learning_records` |
| 数据库列 | snake_case | `created_at` |

### 3.4 API 路由组织规范

```python
# app/api/v1/router.py
from fastapi import APIRouter
from .auth import router as auth_router
from .users import router as users_router
from .agent import router as agent_router
from .summary import router as summary_router
from .exercises import router as exercises_router
from .files import router as files_router
from .knowledge import router as knowledge_router

api_router = APIRouter(prefix="/api/v1")

api_router.include_router(auth_router, prefix="/auth", tags=["认证"])
api_router.include_router(users_router, prefix="/users", tags=["用户"])
api_router.include_router(agent_router, prefix="/agent", tags=["AI Agent"])
api_router.include_router(summary_router, prefix="/summaries", tags=["课文总结"])
api_router.include_router(exercises_router, prefix="/exercises", tags=["习题"])
api_router.include_router(files_router, prefix="/files", tags=["文件管理"])
api_router.include_router(knowledge_router, prefix="/knowledge", tags=["知识图谱"])
```

### 3.5 分层架构规范

每个 API 端点遵循 **Router → Service → Model** 三层：

```python
# 1. Router 层 — 只负责参数解析、调用 Service、返回响应
# app/api/v1/exercises.py
from fastapi import APIRouter, Depends
from app.api.deps import get_current_user, get_db
from app.schemas.exercise import GenerateExerciseReq, GenerateExerciseResp, GradeReq, GradeResp
from app.services.exercise_service import ExerciseService

router = APIRouter()

@router.post("/generate", response_model=list[GenerateExerciseResp])
async def generate_exercises(
    req: GenerateExerciseReq,
    current_user = Depends(get_current_user),
    db = Depends(get_db),
):
    """生成习题 (PBI_08)"""
    service = ExerciseService(db)
    return await service.generate(current_user.id, req)

@router.post("/grade", response_model=GradeResp)
async def grade_answer(
    req: GradeReq,
    current_user = Depends(get_current_user),
    db = Depends(get_db),
):
    """批改作答 (PBI_10)"""
    service = ExerciseService(db)
    return await service.grade(current_user.id, req)

# 2. Service 层 — 业务逻辑，调用 AI/数据库
# app/services/exercise_service.py
class ExerciseService:
    def __init__(self, db: AsyncSession):
        self.db = db

    async def generate(self, user_id: str, req: GenerateExerciseReq) -> list[Exercise]:
        # 1. 构建 Prompt
        # 2. 调用 LLM 生成习题
        # 3. 解析 & 校验
        # 4. 存入数据库
        # 5. 返回
        ...

    async def grade(self, user_id: str, req: GradeReq) -> GradeResult:
        ...

# 3. Model 层 — 数据库映射
# app/models/exercise.py
class Exercise(Base):
    __tablename__ = "exercises"
    id: Mapped[str] = mapped_column(String(36), primary_key=True, default=uuid4)
    user_id: Mapped[str] = mapped_column(ForeignKey("users.id"), index=True)
    subject: Mapped[str] = mapped_column(String(32))
    question: Mapped[str] = mapped_column(Text)
    answer: Mapped[str] = mapped_column(Text)
    difficulty: Mapped[str] = mapped_column(String(16))  # easy/medium/hard
    ...
```

### 3.6 Pydantic Schema 规范

```python
# app/schemas/exercise.py
from pydantic import BaseModel, Field
from datetime import datetime
from enum import Enum

class Difficulty(str, Enum):
    EASY = "easy"
    MEDIUM = "medium"
    HARD = "hard"

class QuestionType(str, Enum):
    CHOICE = "choice"
    FILL = "fill"
    SHORT_ANSWER = "short_answer"
    CALCULATION = "calculation"
    ANALYSIS = "analysis"

# --- 请求 ---
class GenerateExerciseReq(BaseModel):
    subject: str = Field(..., description="学科", examples=["语文"])
    grade: str = Field(..., description="年级", examples=["七年级"])
    knowledge_points: list[str] = Field(..., description="知识点列表")
    difficulty: Difficulty = Field(default=Difficulty.MEDIUM)
    question_types: list[QuestionType] = Field(default=[QuestionType.CHOICE])
    count: int = Field(default=5, ge=1, le=50, description="生成数量")

# --- 响应 ---
class ExerciseItem(BaseModel):
    id: str
    question: str
    question_type: QuestionType
    options: list[str] | None = None   # 选择题选项
    answer: str | None = None          # 做题模式下为 null
    analysis: str | None = None        # 做题模式下为 null
    difficulty: Difficulty
    knowledge_points: list[str]

class GenerateExerciseResp(BaseModel):
    """习题列表（通用响应）"""
    exercises: list[ExerciseItem]

# --- 批改 ---
class AnswerItem(BaseModel):
    exercise_id: str
    user_answer: str

class GradeReq(BaseModel):
    answers: list[AnswerItem]

class GradedItem(BaseModel):
    exercise_id: str
    is_correct: bool
    score: float
    correct_answer: str
    analysis: str
    error_reason: str | None = None
    related_knowledge: list[str] = []

class GradeResp(BaseModel):
    total_score: float
    correct_count: int
    total_count: int
    results: list[GradedItem]
```

### 3.7 依赖注入（Deps）

```python
# app/api/deps.py
from fastapi import Depends, HTTPException, status
from fastapi.security import HTTPBearer, HTTPAuthorizationCredentials
from sqlalchemy.ext.asyncio import AsyncSession
from app.core.database import AsyncSessionLocal
from app.core.security import decode_token
from app.models.user import User

security = HTTPBearer()

async def get_db():
    """每个请求获取独立的数据库会话"""
    async with AsyncSessionLocal() as session:
        try:
            yield session
            await session.commit()
        except Exception:
            await session.rollback()
            raise

async def get_current_user(
    credentials: HTTPAuthorizationCredentials = Depends(security),
    db: AsyncSession = Depends(get_db),
) -> User:
    """从 JWT Token 中解析当前用户"""
    token = credentials.credentials
    payload = decode_token(token)
    user = await db.get(User, payload["sub"])
    if not user:
        raise HTTPException(status_code=401, detail="用户不存在")
    return user
```

### 3.8 日志规范

```python
# 使用 loguru
from loguru import logger

logger.info(f"用户 {user_id} 请求生成习题 | 学科={subject} | 数量={count}")
logger.error(f"LLM 调用失败 | user={user_id} | error={str(e)}")
```

---

## 四、数据库设计概要

### 4.1 核心表结构

```
users                          # 用户表 (PBI_01)
├── id (PK, UUID)
├── email / phone              # 注册方式
├── hashed_password
├── nickname
├── grade                      # 年级
├── subjects                   # JSON: 学科偏好列表
├── textbook_version           # 教材版本
├── avatar_url
├── is_active
├── created_at / updated_at

learning_profiles             # 学习档案 (PBI_01)
├── id (PK)
├── user_id (FK → users)
├── total_study_time           # 累计学习时长(秒)
├── total_exercises            # 累计做题数
├── correct_rate               # 正确率
├── weak_points                # JSON: 薄弱知识点
└── updated_at

chat_sessions                  # AI Agent 对话会话 (PBI_04)
├── id (PK, UUID)
├── user_id (FK → users)
├── title                      # 会话标题
├── created_at / updated_at

chat_messages                  # 对话消息 (PBI_04)
├── id (PK)
├── session_id (FK → chat_sessions)
├── role                       # user / assistant
├── content                    # 文本内容
├── thought_chain              # JSON: 思考链步骤 (PBI_12)
├── tool_calls                 # JSON: 工具调用记录 (PBI_12)
├── created_at

summaries                      # 课文总结记录 (PBI_06)
├── id (PK, UUID)
├── user_id (FK → users)
├── source_type                # text / file
├── source_content             # 原文 / 文件ID引用
├── summary_text               # 总结正文
├── mode                       # brief / detailed
├── knowledge_points           # JSON: 提取的知识点
├── created_at

uploaded_files                 # 上传文件 (PBI_05)
├── id (PK, UUID)
├── user_id (FK → users)
├── filename
├── file_type                  # pdf / docx / txt / md / ...
├── file_size                  # 字节数
├── storage_path
├── parse_status               # pending / processing / done / failed
├── parsed_content             # 解析出的文本内容
├── created_at

exercises                      # 习题 (PBI_08)
├── id (PK, UUID)
├── user_id (FK → users)
├── subject
├── grade
├── question_type
├── question
├── options                    # JSON: 选择题选项
├── answer                     # 标准答案
├── analysis                   # 解题思路
├── difficulty
├── knowledge_points           # JSON
├── created_at

exercise_attempts              # 作答记录 (PBI_09, PBI_10)
├── id (PK, UUID)
├── user_id (FK → users)
├── exercise_id (FK → exercises)
├── user_answer
├── is_correct
├── score
├── graded_by                  # auto / manual
├── created_at

knowledge_graphs               # 知识图谱 (PBI_11)
├── id (PK, UUID)
├── user_id (FK → users)
├── title
├── nodes                      # JSON: 图谱节点
├── edges                      # JSON: 图谱边
├── source_type                # subject / chapter / file
├── created_at
```

---

## 五、API 接口规范

> **Base URL**: `http://localhost:8000/api/v1`  
> **统一响应格式**：见附录  
> **标注 `[SSE]`** 的接口使用 Server-Sent Events 流式返回

### 5.1 认证模块 (PBI_01)

#### 5.1.1 注册

```
POST /auth/register
```

**Request:**
```json
{
  "email": "student@example.com",
  "phone": "13800138000",
  "password": "Abc123456!",
  "grade": "七年级",
  "subjects": ["语文", "数学", "英语"]
}
```

**Response (201):**
```json
{
  "code": 0,
  "message": "注册成功",
  "data": {
    "user_id": "uuid",
    "email": "student@example.com",
    "grade": "七年级"
  }
}
```

#### 5.1.2 登录

```
POST /auth/login
```

**Request:**
```json
{
  "login": "student@example.com",
  "password": "Abc123456!"
}
```

**Response (200):**
```json
{
  "code": 0,
  "data": {
    "access_token": "eyJhbGci...",
    "token_type": "bearer",
    "expires_in": 86400,
    "user": {
      "id": "uuid",
      "email": "student@example.com",
      "nickname": "小明",
      "grade": "七年级",
      "subjects": ["语文", "数学"],
      "textbook_version": "部编版"
    }
  }
}
```

#### 5.1.3 找回密码

```
POST /auth/reset-password
POST /auth/reset-password/verify    # 验证码校验
```

#### 5.1.4 用户资料管理

```
GET    /users/profile                # 获取个人信息
PUT    /users/profile                # 更新个人信息（年级、学科偏好、教材版本）
GET    /users/dashboard              # 学习仪表盘数据（PBI_02）
```

**仪表盘响应:**
```json
{
  "code": 0,
  "data": {
    "total_study_time": 36000,
    "total_exercises": 128,
    "correct_rate": 0.85,
    "recent_summaries": [...],
    "recent_exercises": [...],
    "recommendations": [...],
    "weak_points": ["文言文阅读", "二次函数"]
  }
}
```

---

### 5.2 AI Agent 模块 (PBI_04, PBI_12)

#### 5.2.1 创建对话 `[SSE]`

```
POST /agent/chat
```

**Request:**
```json
{
  "session_id": "uuid-or-null",
  "message": "帮我总结《背影》这篇课文的主要内容，然后出几道阅读理解题"
}
```

**SSE 事件流格式:**

```
data: {"type":"thought","step":1,"title":"需求分析","content":"识别到两个任务：1.课文总结 2.阅读理解题生成"}

data: {"type":"thought","step":2,"title":"调用工具","content":"正在调用【课文总结】工具..."}

data: {"type":"content","chunk":"《背影》是朱自清的散文名篇，主要讲述了..."}

data: {"type":"thought","step":3,"title":"调用工具","content":"正在调用【习题生成】工具，参数：题型=阅读理解，数量=3"}

data: {"type":"content","chunk":"以下是3道阅读理解题：\n1. ..."}

data: {"type":"done","session_id":"uuid","usage":{"tokens":1234}}
```

**前端对接要点：**
- 使用 `EventSource` 或 `fetch` + `ReadableStream` 接收
- `type: "thought"` 渲染到思考面板（PBI_12 可视化区域）
- `type: "content"` 追加到主对话区（打字机效果）
- `type: "done"` 标记结束，保存会话

#### 5.2.2 对话管理

```
GET    /agent/sessions                   # 会话列表
GET    /agent/sessions/{session_id}      # 获取历史消息
DELETE /agent/sessions/{session_id}      # 删除会话
```

---

### 5.3 课文总结模块 (PBI_06)

#### 5.3.1 发起总结 `[SSE]`

```
POST /summaries/generate
```

**Request:**
```json
{
  "source_type": "text",
  "content": "晋太元中，武陵人捕鱼为业...",
  "mode": "detailed",
  "file_id": null
}
```

**SSE 响应流（简化）：**
```
data: {"type":"content","chunk":"## 全文主旨\n\n本文描绘了..."}
data: {"type":"knowledge_points","points":["文言实词","桃花源意象","社会理想"]}
data: {"type":"done","summary_id":"uuid"}
```

#### 5.3.2 总结记录

```
GET    /summaries                # 历史总结列表（分页）
GET    /summaries/{id}           # 查看详情
DELETE /summaries/{id}           # 删除
```

---

### 5.4 文件管理模块 (PBI_05)

#### 5.4.1 文件上传

```
POST /files/upload
Content-Type: multipart/form-data
```

| 参数 | 类型 | 说明 |
|------|------|------|
| `file` | File | 支持 txt, md, pdf, docx, csv, json, html, xml, yaml |
| `auto_parse` | bool | 是否上传后自动解析，默认 true |

**Response (201):**
```json
{
  "code": 0,
  "data": {
    "file_id": "uuid",
    "filename": "初三语文笔记.pdf",
    "file_size": 2048576,
    "file_type": "pdf",
    "parse_status": "processing",
    "created_at": "2026-07-03T10:00:00Z"
  }
}
```

#### 5.4.2 解析状态查询

```
GET /files/{file_id}/status
```

**Response:**
```json
{
  "code": 0,
  "data": {
    "file_id": "uuid",
    "parse_status": "done",
    "parsed_content": "解析后的文本内容...",
    "summary": "一句话摘要",
    "knowledge_points": ["知识点1", "知识点2"]
  }
}
```

#### 5.4.3 文件操作

```
GET    /files                     # 文件列表（分页）
DELETE /files/{file_id}           # 删除文件
POST   /files/{file_id}/reparse   # 重新解析
```

---

### 5.5 习题模块 (PBI_08, PBI_09, PBI_10)

#### 5.5.1 生成习题 `[SSE]`

```
POST /exercises/generate
```

**Request:**
```json
{
  "subject": "语文",
  "grade": "七年级",
  "knowledge_points": ["桃花源记", "文言文翻译"],
  "difficulty": "medium",
  "question_types": ["choice", "fill", "short_answer"],
  "count": 5
}
```

**SSE 响应流：**
```
data: {"type":"progress","generated":1,"total":5}
data: {"type":"exercise","exercise":{...完整习题对象}}
data: {"type":"done","exercises":[...全部习题数组],"batch_id":"uuid"}
```

#### 5.5.2 提交作答 / 批改 (PBI_10)

```
POST /exercises/grade
```

**Request:**
```json
{
  "batch_id": "uuid",
  "answers": [
    {"exercise_id": "uuid-1", "user_answer": "B"},
    {"exercise_id": "uuid-2", "user_answer": "便要还家"},
    {"exercise_id": "uuid-3", "user_answer": "表达了作者对世外桃源的向往..."}
  ]
}
```

**Response:**
```json
{
  "code": 0,
  "data": {
    "total_score": 85.0,
    "correct_count": 4,
    "total_count": 5,
    "results": [
      {
        "exercise_id": "uuid-1",
        "is_correct": true,
        "score": 20.0,
        "correct_answer": "B",
        "analysis": "本题考查...",
        "error_reason": null,
        "related_knowledge": ["文言实词"]
      },
      {
        "exercise_id": "uuid-3",
        "is_correct": false,
        "score": 11.0,
        "correct_answer": "表达了作者对理想社会的向往和对现实社会的不满",
        "analysis": "答题要点：1.向往理想社会 2.对现实不满...",
        "error_reason": "遗漏了'对现实不满'这一要点",
        "related_knowledge": ["桃花源记中心思想", "主观题答题模板"]
      }
    ]
  }
}
```

#### 5.5.3 习题记录

```
GET    /exercises/history               # 做题历史（分页，按时间倒序）
GET    /exercises/batches/{batch_id}    # 单次练习详情（含作答 & 批改）
DELETE /exercises/batches/{batch_id}    # 删除
```

---

### 5.6 知识图谱模块 (PBI_11)

#### 5.6.1 生成知识图谱

```
POST /knowledge/graph
```

**Request:**
```json
{
  "source_type": "subject",
  "source": "语文-七年级-文言文",
  "file_id": null
}
```

**Response:**
```json
{
  "code": 0,
  "data": {
    "graph_id": "uuid",
    "title": "七年级文言文知识图谱",
    "nodes": [
      {"id": "n1", "label": "文言文", "type": "category", "x": 400, "y": 50},
      {"id": "n2", "label": "桃花源记", "type": "article", "x": 200, "y": 200},
      {"id": "n3", "label": "实词", "type": "knowledge", "x": 100, "y": 400},
      {"id": "n4", "label": "虚词", "type": "knowledge", "x": 300, "y": 400}
    ],
    "edges": [
      {"source": "n1", "target": "n2", "relation": "包含课文"},
      {"source": "n2", "target": "n3", "relation": "重点考查"},
      {"source": "n2", "target": "n4", "relation": "重点考查"}
    ]
  }
}
```

#### 5.6.2 图谱操作

```
GET    /knowledge/graphs                       # 图谱列表
GET    /knowledge/graphs/{graph_id}            # 查看图谱
GET    /knowledge/graphs/{graph_id}/node/{node_id}  # 节点详情（知识点、例题、易错点）
DELETE /knowledge/graphs/{graph_id}            # 删除图谱
POST   /knowledge/graphs/{graph_id}/export     # 导出为图片
```

---

## 六、前后端协作规范

### 6.1 API 优先开发流程

```
1. 后端给出 Schema → 2. 前后端评审确认 → 3. 后端实现 + 前端 Mock → 4. 联调
```

1. **后端先行**：定义 Pydantic Schema（请求/响应结构），更新本文档的 API 章节
2. **评审对齐**：前后端一起过 Schema，确认字段名、类型、枚举值、错误码
3. **并行开发**：
   - 后端实现路由 → Service（可先用 Mock 返回）
   - 前端基于 Schema 写 API 层 + Mock 数据 + 页面
4. **联调验证**：后端接口 Ready 后，前端切换 `VITE_API_BASE_URL` 指向真实后端

### 6.2 环境管理

| 环境 | 前端 | 后端 |
|------|------|------|
| 本地开发 | `VITE_API_BASE_URL=http://localhost:8000` | `.env.local` |
| 联调测试 | `VITE_API_BASE_URL=http://test-api.zhiyi.com` | `.env.test` |
| 生产 | `VITE_API_BASE_URL=https://api.zhiyi.com` | `.env.prod` |

### 6.3 Git 分支规范

```
main                  # 生产分支（只接受 PR 合并）
├── develop           # 开发主分支
│   ├── feature/PBI_01-auth     # PBI_01: 认证模块
│   ├── feature/PBI_02-layout   # PBI_02: 导航布局
│   ├── feature/PBI_04-agent    # PBI_04: AI Agent
│   └── ...
└── hotfix/xxx        # 紧急修复
```

**命名规则**：`feature/PBI_XX-功能简称`

**Commit 规范**（Conventional Commits）：
```
feat: 新增用户注册接口 (PBI_01)
fix: 修复文件解析中文乱码问题 (PBI_05)
refactor: 重构 Agent 工具调度逻辑
docs: 更新 API 文档
chore: 升级 FastAPI 到 0.111
```

### 6.4 接口 Mock 方案

后端接口开发期间，前端可使用 [Apifox](https://apifox.com/) 或本地 Mock：

```typescript
// api/request.ts — 开发环境自动 fallback 到 Mock
import { mockAdapter } from './mock'

if (import.meta.env.VITE_USE_MOCK === 'true') {
  mockAdapter(axiosInstance)
}
```

所有 Mock 数据放 `src/api/mock/` 目录，结构与 `modules/` 一一对应，接口联调完成后删除 Mock 文件。

### 6.5 协作 Checklist

联调前，每位开发者确认：

- [ ] 已拉取最新 `develop` 分支
- [ ] 前端 `.env.local` 指向联调后端地址
- [ ] 后端已执行 `alembic upgrade head`（数据库最新迁移）
- [ ] 阅读了当天的 API 文档更新
- [ ] API 字段命名与本文档一致（前后端均以本文档 Schema 为准）
- [ ] SSE 接口在前端使用了正确的流式接收方式

---

## 七、Sprint 分工建议

### 7.1 Sprint 1（第 1-2 周）— 基础建设 + 核心闭环

| PBI | 功能 | 建议负责人 | 前后端占比 |
|-----|------|-----------|-----------|
| 01 | 用户注册登录 + 档案管理 | 开发A | 前端40% / 后端60% |
| 02 | 首页布局 + 导航 + 仪表盘 | 开发B | 前端70% / 后端30% |
| 03 | UI主题 + 字体调节 | 开发B | 前端100% |
| 06 | 智能课文总结 | 开发A | 前端40% / 后端60% |
| 04 | AI Agent 自然语言交互 | 开发C | 前端50% / 后端50% |
| 07 | FastAPI 后端基础设施 | 开发C | 前端0% / 后端100% |

**Sprint 1 目标**：用户能登录 → 看到仪表盘 → 通过 Agent 对话完成课文总结 → **核心闭环走通**

### 7.2 Sprint 2（第 3-4 周）— 进阶功能

| PBI | 功能 | 建议负责人 | 前后端占比 |
|-----|------|-----------|-----------|
| 05 | 文件上传解析 | 开发A | 前端30% / 后端70% |
| 08 | 智能习题生成 | 开发B | 前端30% / 后端70% |
| 09 | 做题/解析双模式 | 开发B | 前端80% / 后端20% |
| 10 | 实时批改反馈 | 开发C | 前端30% / 后端70% |
| 11 | 知识图谱可视化 | 开发A | 前端70% / 后端30% |
| 12 | AI 思考过程可视化 | 开发C | 前端60% / 后端40% |

### 7.3 模块依赖关系

```
PBI_07（后端基础设施）
  └── PBI_01（认证）── PBI_02（导航布局）── PBI_03（UI主题）
        └── PBI_04（AI Agent）── PBI_06（课文总结）
              └── PBI_05（文件上传）── PBI_08（习题生成）
                    └── PBI_09（双模式）── PBI_10（批改）
              └── PBI_12（思考链可视化）
              └── PBI_11（知识图谱）
```

---

## 附录：错误码与通用响应格式

### A.1 统一成功响应

```json
{
  "code": 0,
  "message": "ok",
  "data": { ... }
}
```

### A.2 统一错误响应

```json
{
  "code": 40001,
  "message": "邮箱已被注册",
  "detail": null
}
```

### A.3 错误码速查表

| code | HTTP Status | 说明 |
|------|-------------|------|
| 0 | 200 | 成功 |
| 40001 | 400 | 参数校验失败 |
| 40002 | 400 | 文件格式不支持 |
| 40003 | 400 | 文件大小超限（上限 50MB） |
| 40101 | 401 | Token 过期 |
| 40102 | 401 | Token 无效 |
| 40301 | 403 | 无权操作该资源 |
| 40401 | 404 | 用户不存在 |
| 40402 | 404 | 资源不存在 |
| 40901 | 409 | 邮箱/手机号已被注册 |
| 42901 | 429 | 请求频率超限 |
| 50001 | 500 | 服务器内部错误 |
| 50002 | 500 | LLM 服务调用失败 |
| 50003 | 500 | 文件解析失败 |

### A.4 分页响应

```json
{
  "code": 0,
  "data": {
    "items": [...],
    "total": 128,
    "page": 1,
    "page_size": 20,
    "total_pages": 7
  }
}
```

分页查询统一参数：

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `page` | int | 1 | 页码 |
| `page_size` | int | 20 | 每页条数（最大 100） |

---

> **文档维护约定**：后端每次新增/修改 API，应同步更新本文档的对应接口章节和 Schema。  
> **前端对接约定**：前端以本文档中的 Schema 为数据结构的唯一标准来源，如有不一致请先与后端确认后更新文档。  
> **产品待办列表来源**：`产品待办列表（总需求）(3).xls`，Scrum 模板由 ScrumCN.COM 提供。
